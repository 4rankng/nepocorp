# Phiên bản 0.20

## 🔧 Chuẩn hóa API Response Format

### 📊 Định nghĩa Standard Response Format

Để hỗ trợ pagination và theo best practices của industry, tất cả API endpoints sẽ trả về format chuẩn như sau:

#### Response Format cho List APIs

```
interface ApiResponse<T> {
  data: T[];                    // Mảng dữ liệu chính
  meta: {
    count: number;              // Số records trong response hiện tại
    offset: number;             // Vị trí bắt đầu (0-based indexing)
    limit: number;              // Số records tối đa mỗi trang
    page: number;               // Trang hiện tại (1-based)
    totalPages: number;         // Tổng số trang
  };
  message?: string;             // Thông báo (optional)
  success: boolean;             // Status thành công
}
```

#### Response Format cho Single Record APIs

```
interface ApiSingleResponse<T> {
  data: T;                      // Object dữ liệu
  message?: string;             // Thông báo (optional)
  success: boolean;             // Status thành công
}
```

#### Error Response Format

```
interface ApiErrorResponse {
  error: {
    code: string;               // Mã lỗi
    message: string;            // Thông báo lỗi
    details?: any;              // Chi tiết lỗi (optional)
  };
  success: false;               // Luôn là false cho error
  timestamp: string;            // Thời gian xảy ra lỗi (ISO format)
}
```

## ✨ Cải Tiến Giao Diện Người Dùng (UI Enhancements)

### 🚛 Quản Lý Phương Tiện

- **Triển khai Floating Action Button (FAB) nhất quán**:
  - Áp dụng nút "Thêm" dưới dạng FAB (fixed, bottom-right) cho các tab "Đầu Kéo", "Rơ Mooc", và "Container".
  - Đảm bảo FAB luôn hiển thị, giúp người dùng dễ dàng thực hiện thao tác thêm.
  - Điều chỉnh padding của vùng nội dung để FAB không che khuất các mục cuối danh sách.
  - Chuẩn hóa kích thước (56x56px) và kiểu dáng (bo tròn, hiệu ứng hover, đổ bóng) của FAB cho đồng bộ với các khu vực khác trong ứng dụng (ví dụ: `/doi-tac`).
- **Tối ưu hóa quy trình quản lý**:
  - Cải thiện việc xử lý hộp thoại (dialogs) cho các thao tác thêm, sửa, xóa phương tiện.
  - Tinh gọn mã nguồn và loại bỏ các phần dư thừa để tăng hiệu suất và dễ bảo trì.

### 📝 Quản Lý Định Mức (Norms Management)

- **Đồng bộ hóa trải nghiệm chỉnh sửa**:
  - **Định Mức Bổ Sung, Cho Hàng, Vỏ Rỗng**: Chuyển đổi từ chỉnh sửa trực tiếp (inline editing) sang sử dụng hộp thoại modal để sửa giá trị, mang lại sự nhất quán và cải thiện trải nghiệm người dùng.
- **Cải tiến Định Mức Đi Đường**:
  - Tối ưu hóa việc xử lý và hiển thị dữ liệu liên quan đến định mức đi đường.

### 📅 Quản Lý Lịch Vận Chuyển (Transport Schedule Management)

- **Nâng cao khả năng tìm kiếm và lọc**:
  - Cải thiện chức năng tìm kiếm và lọc trong quản lý lịch vận chuyển, giúp người dùng dễ dàng tìm kiếm thông tin liên quan đến nhân viên và lịch trình.

## ⚙️ Cải Tiến Kỹ Thuật (Technical Improvements)

### 🧪 Mock APIs

- **Chuẩn hóa Mock API**:
  - Sửa và chuẩn hóa các thao tác CRUD (Create, Read, Update, Delete) cho các mock API: `dinhMucDauApi`, `dinhMucDiDuongApi`, `lopXeApi`, và `tuyenDuongApi`.
  - Đảm bảo tính nhất quán trong việc xử lý dữ liệu và phản hồi từ các API giả lập.
