{
  "name": "tingting",
  "private": true,
  "scripts": {
    "dev": "make dev",
    "setup": "make setup",
    "build": "cd shared && npx tsc && cd ../backend && npx tsc && cd ../frontend && npx vite build"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0",
      "uuid": ">=11.1.1"
    }
  },
  "dependencies": {
    "puppeteer": "^25.1.0"
  }
}
