# Iteration 05 — 2026-06-02

**Checklist items:** F3.1–F3.7 batched
**Result:** F3.1, F3.3, F3.4, F3.5, F3.6, F3.7 code-verified; F3.2 BUG (Net column missing in DebtListPage)

## Phase 1 findings

- **F3.1 ✅** `DebtListPage.tsx:245,315` renders "2 chiều" badge when `linkedSupplierId != null`.
- **F3.2 ❌ BUG** No "Net công nợ" column — search of the codebase for `netBalance`/`netDebt`/`Net công nợ` returns 0 matches. The aging response doesn't include linked-supplier AP either, so the FE has nothing to compute net with.
- **F3.3 ✅** `DebtDetailPage.tsx:385,403` shows "Công nợ phải trả (NCC liên kết)" card + "Đối trừ công nợ" button.
- **F3.4 ✅** `DebtOffsetModal.tsx:23` `const offsetAmount = Math.min(arBalance, apBalance)` — pure compute, no editable input.
- **F3.5 ✅** Modal POSTs `/finance/debt-offsets` **without** amount in body (`customerId, supplierId, offsetDate, note`); server computes & lands record PENDING. Banner inside modal confirms: "Quản lý cần phê duyệt để chính thức ghi sổ cái".
- **F3.6 ✅** `debtOffset.service.ts:137,148` posts **two compensating ADJUSTMENT** entries on approval — one debit on customer (reduces AR), one credit on supplier (reduces AP).
- **F3.7 ✅** `DebtDetailPage.tsx:416` renders "Lịch sử đối trừ" table (Ngày/Số tiền/Trạng thái/Ghi chú). Export from DebtListPage's "Xuất báo cáo" button covers the per-pair history channel.

## Phase 2 fix — add Net column

**Files:**
- `backend/src/services/aging.service.ts` — `getCustomerAgingList()` already returns `linkedSupplierId`; extend to also look up that linked supplier's outstanding AP balance and return as `linkedSupplierApBalance`.
- `frontend/src/pages/DebtListPage.tsx` — add a "Net công nợ" column rendering `totalOutstanding − linkedSupplierApBalance` when linked (else `—`).
