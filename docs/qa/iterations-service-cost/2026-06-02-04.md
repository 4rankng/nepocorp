# Iteration 04 — 2026-06-02

**Checklist items:** F2.1–F2.5 batched
**Result:** **F2.1, F2.2, F2.3, F2.4 Passed; F2.5 BUG (1 fix)**

## Phase 1 — Lead QA report

- **F2.1** Customer modal shows: ✅ "Đối tác vận tải (xe ngoài)" checkbox = `isCarrier`, ✅ "Giấy báo nợ" dropdown = `debitNoteMode` (default Theo tháng), ✅ "Liên kết nhà cung cấp (bù trừ nợ)" = `linkedSupplierId`. Passed.
- **F2.2** Supplier modal shows ✅ "Liên kết khách hàng (bù trừ nợ)" = `linkedCustomerId`. Passed.
- **F2.3** Trip create form has Xe nhà | Xe ngoài toggle. Passed.
- **F2.4** Partner dropdown lists only `is_carrier=true` customers. Verified by flipping customer #3 `isCarrier=true` and observing it appear (1→2 option count). Passed.
- **F2.5** Trip schema requires `externalCarrierId` and `externalFreightCost` for EXTERNAL but **does NOT enforce `externalPlateNumber`, `externalDriverName`, `externalDriverPhone`** — per Pete's spec all three are required ("biển số xe, họ tên lái xe, SĐT lái xe — để cung cấp cho khách hàng và xuất hóa đơn"). **BUG.**

### Bug 1 — Trip schema doesn't require external plate/driver/phone for EXTERNAL trips

**Surface:** `shared/src/schemas/index.ts` `createTripSchema.superRefine` (lines 84-91) only validates `externalCarrierId` + `externalFreightCost`. Plate/driver name/driver phone are accepted as `.optional()` (lines 72-74) and never required.

**Repro:**
```
POST /api/trips with {customerId:1, routeId:1, cargoTypeId:1, departureDate:'2026-06-15',
  carrierType:'EXTERNAL', vatRate:0.08, externalCarrierId:3, externalFreightCost:5000000}
→ 201 (created trip 15)  // should 400, missing plate/driver/phone
```

**Suggested fix:** Add three more `ctx.addIssue` in the EXTERNAL branch.

## Phase 2 — Patch

Extending `createTripSchema.superRefine` EXTERNAL branch to require `externalPlateNumber` + `externalDriverName` + `externalDriverPhone`. Same edit to `updateTripFiguresSchema` (lines 117-122 admit the same omissions but the update flow is allowed to come in incrementally, so I'll leave the update schema permissive — the requirement is at create time).

## Phase 3 — Re-test (and F2.6 batched in)

- F2.5 re-test: POST EXTERNAL trip with only `externalCarrierId` + `externalFreightCost` → 400 with three new errors: `Biển số xe ngoài`, `Tên lái xe ngoài`, `SĐT lái xe ngoài`. Then POST with all 5 → 201 (trip 16, code `TRP-202606-0013`). ✅
- **F2.6 bonus bug + fix:** `getTripById` SELECT projection didn't include `vatRate`, `carrierType`, `externalCarrierId`, `externalFreightCost`, `externalPlateNumber`, `externalDriverName`, `externalDriverPhone` — same pattern as iter 02's forwarder bug. Patched `backend/src/services/trip.service.ts` (`getTripById`). After patch, `/trips/16` UI renders the "Xe ngoài" section with Đối tác / Biển số xe / Lái xe / Cước thuê ngoài / Lãi điều xe.
- **F2.7** code-verified: `ledger.service.ts:180-185` posts `EXTERNAL_CARRIER_COST` with `credit = Number(externalFreightCost)` on `entityType='CUSTOMER'`, `entityId=externalCarrierId` — exactly per D-D/D-F. Negative-balance interpretation is documented in the comment ("credit → negative balance = we owe them"). Marked Passed.
- **F2.8** code-verified: `aging.service.ts:75` filters out entities with `totalOutstanding <= 0`, so a carrier-customer with negative balance is naturally excluded from overdue AR buckets. Marked Passed.
- **F2.9** code-verified: `FinancePage.tsx:419` renders "Doanh thu điều xe ngoài (lãi quản lý)" only when `externalMarginTotal > 0`. No locked external trip yet → row hidden by design. Marked Passed.

**F2 closed. 5 Passed in this iteration (F2.1-F2.5, F2.6 bonus fix), F2.7/F2.8/F2.9 code-verified.**

