# Iter 05 — M6.1–M6.4: Lock EXTERNAL trip & verify ledger postings

**Items:** M6 (genuine gap — SCC-F2.7 was code-only)
**Role:** MANAGER `phung`
**Date:** 2026-06-02
**Method:** Live end-to-end run; ledger inspected via DB

## Setup

- Customer 9 (VẬN TẢ TEST) flipped `isCarrier=true`, `debitNoteMode=MONTHLY` via PUT /api/customers/9.
- Created trip 18 (`TRP-202606-0015`) as MANAGER `phung`:
  ```json
  {customerId:1 (NITODA), routeId:3, cargoTypeId:1, carrierType:"EXTERNAL",
   externalCarrierId:9, externalFreightCost:5400000, externalPlateNumber:"29A-99999",
   externalDriverName:"Nguyễn Văn Test", externalDriverPhone:"0987654321",
   revenue:10800000, vatRate:0.08, ...}
  ```

## M6.1 — State transitions

- `POST /api/trips/18/dispatch` → status=IN_TRANSIT ✅
- `PUT /api/trips/18/actuals` (with legs) → stayed IN_TRANSIT (auto-complete requires a photo upload, `trip.service.ts:361-368`) — observation, not a bug; the test guide flow assumes photos are taken when truck is back. For this audit-only test I bumped COMPLETED via DB.
- `POST /api/trips/18/lock` → **status=LOCKED** ✅, ledger postings triggered.

## M6.2 — Carrier ledger CREDIT entry

```sql
SELECT id, txn_type, entity_type, entity_id, credit, debit, balance, note
FROM ledger WHERE txn_id = 18;
```
```
 id |       txn_type        | entity_type | entity_id | credit  | debit    | balance  | note
----+-----------------------+-------------+-----------+---------+----------+----------+--------------------------
 23 | TRIP_REVENUE          | CUSTOMER    |    1      |       0 | 10800000 | 11800000 | Doanh thu chuyến TRP-…
 24 | EXTERNAL_CARRIER_COST | CUSTOMER    |    9      | 5400000 |        0 | -5400000 | Cước thuê ngoài chuyến…
```

✅ Carrier (cust 9) gets CREDIT 5,400,000 → running balance -5,400,000 (company owes carrier). Matches the test guide ("Số dư sổ cái của đối tác này sẽ âm").

## M6.3 — No driver-salary entry

```sql
SELECT COUNT(*) FROM ledger WHERE txn_id = 18 AND entity_type = 'DRIVER';
```
```
 driver_entries
----------------
              0
```
✅ Zero driver-salary entries. EXTERNAL trips do not post driver compensation.

## M6.4 — Customer DEBIT for revenue

✅ Entry 23: `TRIP_REVENUE` posted on customer 1 (NITODA, the buyer), DEBIT 10,800,000 — accounts receivable booked normally regardless of the dispatch mode.

## Verdict

- M6.1 → Passed (lock route works; auto-complete-from-actuals requires photo upload — out-of-scope nuance)
- M6.2 → Passed
- M6.3 → Passed
- M6.4 → Passed

0 bugs filed.
