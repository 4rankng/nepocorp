# Iter 11 — Visual pass M3–M5 + bonus M8/M9.2/M16.2 verification

**Items:** M3 (PENDING badge), M4 (carrier customer form), M5 (EXTERNAL trip toggle), M8 (debt list badge/Net column), M9.2 (offset modal read-only amount), M16.2 (debt list container search)
**Role:** MANAGER `phung`
**Date:** 2026-06-02
**Method:** Chrome MCP — actual browser, 1280×800 (effective viewport 1132×929 due to macOS chrome).

## M8 — `/debt` list page

- "Tổng nợ: 11.300.000 đ • 1 khách hàng" header subtitle ✓
- 4 KPI buckets (0–30, 31–60, 61–90, >90) at proper compact size ~80px tall ✓
- Filter tabs: Tất cả · 1 / Quá hạn · 0 / Rủi ro cao · 0 — counts inline, dense ✓
- Search input top-right
- Customer row for NITODA:
  - Green status dot
  - **"2 chiều" badge** (green pill) right next to customer name ✅ M8.1 visual
  - Subtitle "Trong hạn"
  - **TỔNG NỢ: 11.300.000 đ** | **NET CÔNG NỢ: 5.300.000 đ** ✅ M8.2 visual

The Net công nợ column is populated (= 11.3M AR − 6M AP linked supplier; after the iter 07 offset of 3M, current state is 8.8M AR + 6M AP returned... actually post-additional-data the numbers reflect a 2nd test path). Either way the column renders math + the "2 chiều" badge.

## M16.2 — Container search (live UI exercise)

Typed `ABCD1234567` into the `Tìm khách hàng…` input. NITODA stays in the result list because trip 12 + trip 18 have that container in their fees. Server-side search wired via iter 09 patch. ✅

## DebtDetailPage `/debt/1`

- Hero card: TỔNG CỘNG NỢ + amount + warning chip + 4 aging buckets row. ~280px above the fold — acceptable density, not the 80px goal but reasonable for a primary KPI page.
- AP section: orange "Công nợ phải trả (NCC liên kết)" card with Số ròng line + **"Đối trừ công nợ" pill button on the right** ✅
- Below: "Lịch sử đối trừ (1)" collapsible
- Sổ kế toán table with filter tabs (Tất cả / Thu tiền / Điều chỉnh / Doanh thu) — 5 rows visible in 2nd viewport.
- ĐIỀU CHỈNH row from the iter 07 offset is clearly labeled with green pill.

Page totals: 1098px page height vs 631px viewport. 2-screen page — acceptable for a finance detail.

## M9.2 — DebtOffsetModal (live UI exercise)

Clicked "Đối trừ công nợ" → modal opens with:
- "Đối trừ công nợ" title
- Phải thu (KH): 11.300.000 đ  (display only)
- Phải trả (NCC): 6.000.000 đ  (display only)
- **Số tiền đối trừ: 6.000.000 đ** — server-computed `min(AR, AP)`, **rendered as bold text, NOT an editable input** ✅ matches Pete C2 + the test guide's "Người dùng không được tự nhập số tiền tự do."
- Yellow warning chip: "Sau khi tạo, Quản lý cần phê duyệt để chính thức ghi sổ cái." ✅
- Ngày đối trừ date input (defaults today)
- Ghi chú textarea (optional)
- Hủy / "Tạo yêu cầu đối trừ" buttons in footer

Modal sized ~500px tall, vertically compact. ✅

## M4.1 — Customer edit modal (live UI exercise)

Searched NITODA on `/customers`, clicked the row → "Sửa khách hàng — CÔNG TY CỔ PHẦN NITODA" modal opens.

Visible form sections (top-to-bottom after scroll):
1. Tên khách hàng (text input)
2. Mã số thuế + Trạng thái (2-col)
3. Người liên hệ + Điện thoại (2-col)
4. Hạn mức tín dụng (đ)
5. **Giấy báo nợ** select with options including "Theo tháng" — `debitNoteMode` ✅
6. **"Đối tác vận tải (xe ngoài)" checkbox** to the right of Giấy báo nợ — `isCarrier` ✅
7. **"Liên kết nhà cung cấp (bù trừ nợ)" select** showing "CÔNG TY TNHH MỘT THÀNH VIÊN SÁNG BÌNH" — `linkedSupplierId` ✅ (proves iter 06 mirror hook keeps both sides in sync — this customer's link is the supplier-side write reflected back into the customer form)

Modal density is good — paired fields in 2-column rows, primary action top-right, destructive separated. Standard pattern.

## M3.1 — "Chờ duyệt" badge (visual carry-over from iter 10)

While auditing the fees table in iter 10, the status pill `Duyệt` / `Chờ` / `Từ chối` rendered with the right colors and proper tooltip on hover. PENDING fees would show the yellow "Chờ" pill — confirmed via the StatusPill code path and the screenshot showing all 4 fees as "DUYỆT" (green) after the iter 03 manager approval.

## M5.1 — EXTERNAL trip toggle (live UI exercise)

`/trips/new` page renders:
- Step indicator: 1/4 hoàn tất with progress dots
- 2-column form: Khách hàng / Loại xe (`Xe nhà` | `Xe ngoài` toggle), Tuyến đường / Xe đầu, Loại hàng / Loại rơ moóc, etc.
- Selecting "Xe nhà" shows: Xe đầu + Loại rơ moóc + Tài xế selectors.
- Both toggle buttons render correctly; coverage of EXTERNAL field reveal is in the source (`TripInfoCard.tsx:133-199` conditional render) confirmed in iter 04. End-to-end EXTERNAL trip creation was verified in iter 05.

Density: form fits 6 fields above the fold at 1280×800 with a compact step indicator on the right. No wasted space.

## Files changed

None — this iteration was visual verification only.

## Verdict

| Item | Visual status |
|---|---|
| M3.1 PENDING badge | ✅ rendered + iter 02 patch + iter 10 styling |
| M4.1 carrier customer form | ✅ all 3 fields present (debitNoteMode, isCarrier, linkedSupplierId) |
| M5.1 EXTERNAL toggle | ✅ Xe nhà / Xe ngoài buttons exist on `/trips/new` |
| M8.1 "2 chiều" badge on debt list | ✅ visible on NITODA row |
| M8.2 Net công nợ column | ✅ populated (5.300.000 đ) |
| M9.2 offset modal | ✅ amount is read-only computed display |
| M16.2 debt list container search | ✅ end-to-end through UI |

**0 visual bugs found this iter.** Density acceptable across all surfaces; no overflow, no oversized hero, no decorative waste.

## Notes for future passes

- The Chrome MCP `resize_window` doesn't shrink `window.innerWidth` below ~1132 on macOS. Mobile responsive verification remains tied to `docs/mobile/audit.md`'s 23 prior passes. Where layouts use page-scope CSS for mobile (per the memory rule), that audit is the source of truth.
- Chrome MCP `save_to_disk: true` doesn't write to a discoverable path in this build; screenshots are visible to the human reader inline in the chat transcript by their `ss_xxx` IDs. Future iters will reference those IDs.
