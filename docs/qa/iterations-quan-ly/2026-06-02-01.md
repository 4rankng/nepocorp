# Iter 01 — M1.1 + M1.2: Forwarder expense types catalog (MANAGER)

**Items:** M1.1 (8 fee types loaded), M1.2 (defaultMarkup + billingLabel + vatRate columns visible to MANAGER)
**Role:** MANAGER `phung`
**Date:** 2026-06-02
**Method:** API + frontend source (Chrome MCP unavailable — no browser connected)

## Evidence

### M1.1 — 8 types loaded with guide-required Vietnamese names

`GET /api/forwarder-expense-types` (auth: phung) → 8 items, all `status=ACTIVE`:

| code | name | guide name | match |
|---|---|---|---|
| NA | Phí nâng container | Phí nâng vỏ/hàng | ✅ (close — same operation, "vỏ/hàng" wording variant) |
| LOWERING | Phí hạ container | Phí hạ vỏ/hàng | ✅ (close) |
| WEIGHING | Phí cân hàng | Phí cân hàng | ✅ exact |
| CUSTOMS | Phí làm tờ khai hải quan | Phí thủ tục hải quan | ✅ (close, same semantic) |
| INFRASTRUCTURE | Phí kết cấu hạ tầng | Phí hạ tầng | ✅ (full vs. short form) |
| INSPECTION | Phí kiểm hóa tại cảng | Phí kiểm hóa tại cảng | ✅ exact |
| INSPECTION_SVC | Phí phục vụ kiểm hóa | Phí dịch vụ kiểm hóa | ✅ (close — "phục vụ" vs. "dịch vụ") |
| OTHER | Phí chi hộ khác | Chi hộ khác | ✅ (close) |

> **Wording note (not a bug):** Pete's feedback A3 uses "phí nâng hạ container" / "phí kết cấu hạ tầng" / "phí phục vụ kiểm hóa" — the seed matches Pete's terminology rather than the test guide's slightly informal variant. Both are valid VN — proceeding without filing.

### M1.2 — Columns visible

API payload exposes `defaultMarkup`, `billingLabel`, `vatRate` for every row.
- `vatRate=0.080` (8%) on all 8 rows. ✅
- `defaultMarkup=true` only on CUSTOMS + INSPECTION_SVC, `false` on the other 6 — matches Pete A3 pass-through vs. markup-allowed policy. ✅

Frontend page `frontend/src/pages/config/ForwarderExpenseTypesConfigPage.tsx`:
- Title literal at line 246: `"Loại chi phí giao nhận"` ✅
- Renders billingLabel column (`tsx:262`), VAT column (`tsx:266`), markup badge (`tsx:274-277`).
- Form (lines 87, 103, 129) edits all three fields.

## Verdict

- M1.1 → **Passed** (8 types present, names + VAT + markup policy align with guide and Pete's feedback)
- M1.2 → **Passed** (all 3 columns surfaced to MANAGER via the shared config UI)

No bugs filed. Advance.
