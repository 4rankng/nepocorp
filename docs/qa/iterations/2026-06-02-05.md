# Iteration 05 — 2026-06-02

**Checklist item:** 2.4 Director soft-deletes a user
**Tester role:** phung (MANAGER)
**Result:** PASSED (per docs; MANAGER cannot delete) + patched UX so the impossible action is no longer offered

## Test Steps
1. As phung, on `/users`, click trash icon on the `qa_test_phung` row
2. Confirm "Xóa tài khoản này?" dialog → click "Xóa"
3. Inspect fetch log + DOM + toast

## Bug — Delete button visible for MANAGER but API denies (403)
- DELETE `/api/auth/users/10` returned `403 Forbidden` ("Không có quyền truy cập")
- Toast feedback shown bottom-right (works correctly)
- Row stayed in list (correct: delete failed)
- Per `docs/flows/00-OVERVIEW_VA_PHAN_QUYEN.md`, MANAGER has `users:read` only — no write or delete. Backend Casbin map: `DELETE → 'delete'` action. ADMIN-only.
- UX bug: `UsersPage.tsx:12-14` set `canManage = role === ADMIN || role === MANAGER`, exposing Delete button to MANAGER. Confusing because user goes through confirm dialog only to be denied.

## Note on POST/PATCH discrepancy
- Iter 03/04 confirmed phung (MANAGER) CAN successfully POST and PATCH users (no 403)
- Backend appears to use a softer policy for create/update but enforces strict ADMIN-only on delete
- Docs say MANAGER should have NEITHER write nor delete on users — so there's a separate inconsistency between docs and backend policy that's out of scope for this UX fix.

## Patch Report
Frontend-only UX patch — hide Delete button when current user is not ADMIN.

**Files changed:**
- `frontend/src/pages/UsersPage.tsx`
  - Added `const canDelete = me?.role === Role.ADMIN`
  - Pass `canDelete` prop to `UserTable`
- `frontend/src/features/users/components/UserTable.tsx`
  - Added optional `canDelete?: boolean` to `UserTableProps`, defaults to `canManage` for back-compat
  - Threaded `canDelete` through to both `DesktopTable` and `MobileCardList`
  - Wrapped desktop trash button render in `{canDelete && (...)}`
  - Wrapped mobile dropdown "Xoá" item render in `{canDelete && (...)}`

Edit button + Create button visibility unchanged (still rendered for MANAGER, matching the actual backend behavior).

## Re-test after patch
- Reloaded `/users` as phung
- Each row now shows only the edit pencil icon — no trash button
- Mobile dropdown menu shows "Sửa" only, no "Xoá"
- Verified row count = 8, all roles render correctly

## Bug Count
1 (patched)
