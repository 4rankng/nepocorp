# Iteration 06 — 2026-06-02

**Checklist item:** 2.5 Accountant /users → API 403 (or hidden in sidebar)
**Tester role:** anh (ACCOUNTANT)
**Result:** PASSED

## Test Steps
1. POST `/api/auth/login` with `anh/admin123` → token + role=ACCOUNTANT
2. Set token in localStorage, navigate to `/users`
3. Verify sidebar contents and route behavior
4. Direct API call `GET /api/auth/users`

## Findings
- Direct nav to `/users` → frontend route guard redirects to `/dashboard`
- Sidebar for ACCOUNTANT does NOT show "Người dùng" or "Nhật ký người dùng" links (hidden via role-based render)
- Direct API call `GET /api/auth/users` returns `403 Forbidden`
- Combined frontend + backend enforcement — exactly what the spec wants

## Note on test-account password
The HTML form-based login through Chrome MCP was flaky (token wasn't persisted reliably across the immediate JS check), but direct `POST /api/auth/login` with `admin123` succeeded for `anh`. The credentials are correct; the form-submit timing was the issue. No defect.

## Patch Report
No patch required.

## Bug Count
0
