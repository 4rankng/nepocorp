# Iteration 03 — 2026-06-02

**Checklist item:** 2.2 Director creates new user (RETRY after iter 02 anomaly)
**Tester role:** phung (MANAGER)
**Result:** PASSED

## Test Steps
1. Logout, login as `phung`/`admin123`
2. Confirm `/api/auth/me` returns `{role: "MANAGER", username: "phung"}`
3. Navigate to `/users`, instrument `window.fetch` to log all calls
4. Click "Thêm tài khoản"
5. Fill form: name=`QA Test Phung`, username=`qa_test_phung`, email=`qa_phung@test.local`, phone=`0900111222`, role=DRIVER, password=`qatest123`
6. Click "Tạo tài khoản"
7. Inspect fetch log + row count

## Findings
- `POST /api/auth/users` fired (instrumented `window.fetch` confirms)
- Followed by `GET /api/auth/users` (the list refetch)
- Row count went 7 → 8
- Drawer auto-closed on success
- New user `qa_test_phung` visible in the table

## Resolution of iter 02 false bug
Iter 02's "no POST observed" was a **chrome-MCP network-log artifact**, not a real defect. The role-flip mid-test caused a route redirect to `/my-forwarder-trips`, which appears to have flushed/clobbered the network log entries before they could be read. With explicit `window.fetch` instrumentation, the POST is plainly visible. Bug #1 from iter 02 is **withdrawn**.

The environmental anomaly (quan demoted MANAGER → FORWARDER, user list overwrite) remains unexplained — likely an external reseed/migration during the session. Not a frontend defect.

## Patch Report
No patch required. Both the form wiring (`AddPanel.handleSubmit` → `doCreate` → `userClient.createUser` → `POST /api/auth/users`) and the success refresh are working correctly.

## Bug Count
0
