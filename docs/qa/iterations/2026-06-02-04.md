# Iteration 04 — 2026-06-02

**Checklist item:** 2.3 Director edits existing user
**Tester role:** phung (MANAGER)
**Result:** PASSED

## Test Steps
1. On `/users`, click edit icon on the `qa_test_phung` row (created in iter 03)
2. Confirm drawer opens with pre-filled values (fullName=`QA Test Phung`, username=`qa_test_phung`, role=DRIVER)
3. Change fullName to `QA Test Phung Edited` and role to FORWARDER
4. Click "Lưu thay đổi"
5. Inspect fetch log + table

## Findings
- Drawer opens with correct pre-filled values
- `PATCH /api/auth/users/10` fires
- `GET /api/auth/users` refresh follows
- Table row updates: name → `QA Test Phung Edited`, role → `Giao nhận` (FORWARDER)
- Drawer auto-closes

## Patch Report
No patch required.

## Bug Count
0
