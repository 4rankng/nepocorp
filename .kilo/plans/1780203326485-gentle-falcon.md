# Plan: Fix AGENTS.md to match actual project structure

## Problem
AGENTS.md references `packages/` paths everywhere but the actual project uses a flat workspace layout (`backend/`, `frontend/`, `shared/` at repo root). Multiple other details are also stale (ports, .env path, RBAC middleware, frontend page status, dev commands, route list, etc.).

## Changes

### 1. Commands section
- `pnpm dev` → `make dev` (root package.json has no dev scripts, uses Makefile)
- `pnpm dev:backend` → `cd backend && pnpm dev`
- `pnpm dev:frontend` → `cd frontend && npx vite --port 7173`
- `pnpm build` → `make build` (or `pnpm build` which does inline cd chain)
- `pnpm db:*` → `cd backend && pnpm db:generate|migrate|studio`
- `.env` path: `packages/backend/.env.example` → `backend/.env.example`
- Add `make setup`, `make seed`, `make studio` commands

### 2. Architecture tree
```
packages/
├── shared/
├── backend/
└── frontend/
```
→
```
backend/
frontend/
shared/
```

### 3. Backend section header
`packages/backend/src/` → `backend/src/`

### 4. Backend path table updates
- Add `middleware/casbin.ts` — Casbin RBAC middleware + `requireRoles()` (moved from `auth.ts`)
- `middleware/auth.ts` — JWT verification only (requireRoles moved to `casbin.ts`)
- Add `middleware/errorHandler.ts` — global error handler (Zod → 400, ApiError, PG unique → 409, generic → 500)
- Add `middleware/serializer.ts` — snake_case response serializer
- Add `middleware/logger.ts` — request logger
- Add `routes/maps.ts` to route list
- Add `services/` entries: `ledger.service.ts`, `reporting.service.ts`, `receivables.service.ts`, `statement.service.ts`, `salary-period.service.ts`, `config.service.ts`, `salary-period.service.ts`, `audit.service.ts`, `driver.service.ts`, `storage.service.ts`
- Add `errors.ts` — `ApiError` class
- Add `casbin/` — Casbin enforcer setup

### 5. Frontend section header
`packages/frontend/src/` → `frontend/src/`

### 6. Frontend path table updates
- `api/` is empty → remove or update (verify)
- `hooks/` — now has 12 hooks (useAuditLogs, useAuth, useCatalogs, useClickOutside, useCRUD, useFleetData, useObservedWidth, usePenalties, useQueries, useTripForm, useTripFormContext, useTripOptions)
- `lib/` — now has 9 files (api, avatar, cap-table, csv, date, format, maps, round, route)
- `components/` — now has Layout, UI, shared/, trip/, TripForm/, config/, LocationAutocomplete
- `pages/` — fully implemented (36 files), NOT stubs anymore

### 7. Shared section header
`packages/shared/src/` → `shared/src/`

### 8. Shared path table updates
- Add `calculations/` directory
- Add `index.ts` barrel export

### 9. Backend conventions
- RBAC: `requireRoles()` is in `middleware/casbin.ts`, NOT `middleware/auth.ts`. `auth.ts` only does JWT.
- Casbin policy-based auth: `casbinAuthz('resource')` wraps most route groups
- Add: `globalErrorHandler` catches ZodError → 400, ApiError → custom status, PG 23505 → 409, generic → 500
- Add: `snakeCaseSerializer` middleware converts response keys
- Add: `createCrudRouter()` factory in `services/config.service.ts`

### 10. Frontend conventions
- Port is 7173 (not 5173), proxies to 3090 (not 3001)
- Frontend pages are fully implemented, not stubs
- Multiple hooks beyond useAuth
- Components organized in subdirectories (trip/, shared/, config/, TripForm/)

### 11. Current State section
- Frontend: NOT stubs anymore — fully implemented with many pages
- Backend port: 3090 not 3001
- Tests: backend has `npm test` → `npx tsx --test src/tests/integration.test.ts`

### 12. .env path
`packages/backend/.env.example` → `backend/.env.example`
